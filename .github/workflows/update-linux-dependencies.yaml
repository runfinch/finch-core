name: update-linux-dependencies

on:
  workflow_call:
  workflow_dispatch:

jobs:
  update-dependencies-and-create-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      
      - name: Run dependency update
        run: |
          cd bin
          chmod +x update-linux-dependencies.sh
          ./update-linux-dependencies.sh

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet deps/ubuntu-deps.conf deps/amazon-linux-deps.conf; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No changes detected in dependencies"
            exit 0
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Changes detected in dependencies:"
            git diff deps/ubuntu-deps.conf deps/amazon-linux-deps.conf
          fi

      - name: Create or update PR
        if: github.event_name != 'pull_request' && steps.changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@271a8d0340265f705b14b6d32b9829c1cb33d45e # v7.0.8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          signoff: true
          commit-message: 'chore: update linux dependencies'
          title: 'chore: update linux dependencies'
          body: |
            This PR updates the following dependencies to their latest versions:
            
            - finch-daemon (for both Ubuntu & AL)
            - nerdctl (for Ubuntu only)
            - buildkit (for both Ubuntu & AL)
            - soci-snapshotter (for both Ubuntu & AL)
            - CNI plugins (for Ubuntu only)
            - cosign (for both Ubuntu & AL)
            
            This is an automated update created by the dependency update workflow. Review the changes before approving.
          branch: update-linux-dependencies
          delete-branch: true
